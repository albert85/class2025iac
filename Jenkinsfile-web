pipeline {
    agent any 
    stages {
        stage ('checkout') {
            steps {
                checkout scm
            }
        }
        
        stage ('build') {
            sshagent (credentials: ['master-key']) {
                sh """
                    ssh -o StrictHostKeyChecking=no ec2-user@172.31.9.65 'sudo yum install git -y; sudo yum install nginx -y; sudo systemctl start nginx; sudo systemctl enable nginx;'
                """
            }
        }
    }
}